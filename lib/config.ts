/**
 * App-level constants. The MiniPay listing form asks for a support link, a
 * terms link and a privacy link that are all reachable from inside the app.
 */

export const APP_NAME = "SafeSign";

/** Update if the repository is published under a different name or owner. */
export const SUPPORT_URL = "https://github.com/luhrhenz/safesign/issues";

export const SUPPORTED_CHAIN_LABELS = "Celo, Base, Ethereum and BNB Chain";
